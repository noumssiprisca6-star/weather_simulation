#include "renderer.h"
#include <stb_image.h>



void DrawScene(SDL_Renderer* renderer, Meteo meteo) {
    if (meteo == Meteo::Soleil) {
        // draw sun
    }
    if (meteo == Meteo::Pluie) {
        // draw rain
    }
}